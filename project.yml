name: Cue
options:
  bundleIdPrefix: mit.shenshen.cue
  deploymentTarget:
    iOS: "17.0"
    watchOS: "11.0"
  createIntermediateGroups: true
  groupSortPosition: top

settings:
  base:
    DEVELOPMENT_TEAM: ""
    SWIFT_VERSION: "5.10"
    GENERATE_INFOPLIST_FILE: NO
    CURRENT_PROJECT_VERSION: 1
    MARKETING_VERSION: "0.1"
    CODE_SIGN_STYLE: Automatic

targets:
  Cue:
    type: application
    platform: iOS
    sources:
      - path: Shared
      - path: iOS
        excludes:
          - "Resources/Info.plist"
    resources:
      - path: iOS/Resources
        excludes:
          - "Info.plist"
    info:
      path: iOS/Resources/Info.plist
      properties:
        CFBundleDisplayName: Cue
        UILaunchScreen: {}
        UIRequiredDeviceCapabilities:
          - armv7
        UIApplicationSupportsIndirectInputEvents: true
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSMicrophoneUsageDescription: "Used to capture spoken comments during lectures."
        NSSpeechRecognitionUsageDescription: "Used to transcribe your spoken comments."
        WKCompanionAppBundleIdentifier: mit.shenshen.cue.watchkitapp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: mit.shenshen.cue
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: NO
        ENABLE_PREVIEWS: YES
    dependencies:
      - target: CueWatch
        embed: true
        codeSign: true

  CueWatch:
    type: application
    platform: watchOS
    sources:
      - path: Shared
      - path: Watch
        excludes:
          - "Resources/Info.plist"
    resources:
      - path: Watch/Resources
        excludes:
          - "Info.plist"
    info:
      path: Watch/Resources/Info.plist
      properties:
        CFBundleDisplayName: Cue
        WKApplication: true
        WKWatchOnly: false
        WKRunsIndependentlyOfCompanionApp: false
        NSMicrophoneUsageDescription: "Used to capture spoken comments during lectures."
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: mit.shenshen.cue.watchkitapp
        TARGETED_DEVICE_FAMILY: "4"
        WATCHOS_DEPLOYMENT_TARGET: "11.0"
        SDKROOT: watchos
        SUPPORTED_PLATFORMS: "watchos watchsimulator"
